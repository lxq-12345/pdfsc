# 会话备忘 - 2026-04-05（D2诊断与修复阶段进展）

**日期**：2026-04-05  
**项目**：PDF智转器（pdfsc）  
**主题**：D2诊断与修复 - 流程优先阶段收口

---

## 一、本次完成事项

1. 增加 mock 适配器能力（流程联调专用）
- 在模型适配层新增 `provider=mock`
- mock 模式不发起外部网络请求，返回稳定占位结果

2. 增加低质量标记机制
- 在 `offline / fallback / mock` 场景自动插入：
  - `<!-- DRAFT/LOW_QUALITY -->`
  - `DRAFT/LOW_QUALITY` 提示说明
- 防止流程验证输出被误当最终交付

3. 增强批量报告可观测性
- 在批量汇总报告中新增：
  - 低质量命中统计
  - restore/enhance 运行模式分布
  - 单文件运行模式与低质量命中字段

4. 新增 D2 一键执行入口
- 新增 `scripts/run_d2.sh`
- 支持：
  - `flow-test`
  - `convert <pdf> [mock|offline|real]`
  - `batch <dir> [mock|offline|real]`
- 新增默认配置 `config/d2_flow.yml`

5. 新增测试并回归
- `tests/test_d2_mock_flow.py`：验证 mock + 低质量标记链路
- `tests/test_d2_batch_summary.py`：验证批量报告统计字段
- 回归 `tests/test_stage_b2.py` 通过

---

## 二、关键结论

- D2 当前已完成“流程打通 + 可观测性增强”的第一目标。  
- 项目仍处于 D2 阶段，后续需进入“质量修复与验收冲刺”。

---

## 三、建议下一步

1. 以 `run_d2.sh` 为入口，先在目标样本上完成 `mock -> offline -> real` 分层跑通
2. 进入质量修复主线：restore prompt 强化 + 长文档分段策略
3. 继续以 D2 样本集做回归，逐步收敛到验收标准
